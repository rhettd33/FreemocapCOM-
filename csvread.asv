clf
table = readtable('data.csv');
full = table2array(table);

hold on

for row = 1:214
    X = full(row, 1);
    Y = full(row, 2);
    
    
    
    X2 = full((row+2), 1)
    Y2 = full((row+
    
    
    
    axis([-500, 500, -500, 500])
    scatter(X,Y,'filled')
    %pause(.01)
    
    
end
hold off
